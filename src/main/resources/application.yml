spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          # Keep Auth0 configuration in Render instead of source-controlled files.
          issuer-uri: ${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI:https://formex-payment.us.auth0.com/}
